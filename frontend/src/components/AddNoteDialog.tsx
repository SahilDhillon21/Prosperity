import { Modal } from "react-bootstrap"

export const AddNoteDialog = () => {
  return (
    <Modal show={true}>
        <Modal.Header closeButton>
            
        </Modal.Header>
    </Modal>
  )
}
